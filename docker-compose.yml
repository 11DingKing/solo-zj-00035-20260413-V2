services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_JPA_HIBERNATE_DDL_AUTO=create-drop

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
